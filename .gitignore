/group_vars/all/vault.yml
# TODO: consider adding more standard ignores:
# *.retry        — Ansible creates these files when a playbook run fails
# *.log          — log files
# __pycache__/   — Python bytecode cache (Ansible uses Python under the hood)
# .env           — local environment variable files that may contain secrets
