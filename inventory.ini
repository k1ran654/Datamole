# use the -K flag to enter the become password

[myhosts]
# TODO: Never commit credentials to git — even if the repo is private today, it may not be tomorrow.
# Prefer SSH key authentication: remove ansible_password entirely and ensure your SSH key is
# authorised on the target host. If a password is truly needed, store it in Ansible Vault:
#   ansible-vault encrypt_string '12345' --name 'ansible_password'
# and reference it as a group_var so secrets never live in plain text.
# TODO: "12345" is an extremely weak password. Use a strong, randomly generated one.
# TODO: rotate the password on the host, it is leaked now.
10.7.103.223 ansible_user=jirka_srv ansible_password=12345
